import { ShowMedicinePipe } from './show-medicine.pipe';

describe('ShowMedicinePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMedicinePipe();
    expect(pipe).toBeTruthy();
  });
});
